events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Configuración de logs
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    sendfile on;
    keepalive_timeout 65;

    # Compresión gzip
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json image/svg+xml;

    server {
        listen 81;
        server_name localhost;
        root /usr/share/nginx/html;
        index index.html index.htm;

        # Servir imágenes y assets estáticos (funciona con y sin base-href)
        # Esta regla debe ir ANTES de las location generales para tener prioridad
        location ~ ^/(sistema-seguridad/)?img/ {
            root /usr/share/nginx/html;
            rewrite ^/sistema-seguridad/img/(.*)$ /img/$1 break;
            expires 1y;
            add_header Cache-Control "public, immutable";
            access_log off;
        }

        # Cache para assets estáticos (debe ir antes de location /)
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        # Deshabilitar cache para index.html
        location = /index.html {
            add_header Cache-Control "no-cache, no-store, must-revalidate";
            expires 0;
        }

        # Configuración para Angular SPA (Single Page Application)
        # Manejar rutas con base-href /sistema-seguridad/
        location /sistema-seguridad/ {
            try_files $uri $uri/ /sistema-seguridad/index.html;
        }

        # Redirección de raíz a sistema-seguridad si es necesario
        location = / {
            return 301 /sistema-seguridad/;
        }

        # Fallback para todas las demás rutas (redirigir a sistema-seguridad)
        location / {
            try_files $uri $uri/ /sistema-seguridad/index.html;
        }

        # Headers de seguridad
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "no-referrer-when-downgrade" always;

        # Manejo de errores
        error_page 404 /index.html;
    }
}

